import { css } from 'emotion/macro';

const toolbar = css`
  margin: 8px 8px 8px 0;
`;

const toolbarSection = css`
  margin-right: 12px;
`;

const toolbarSelect = css`
  width: 160px;
`;

export default {
  toolbar,
  toolbarSection,
  toolbarSelect,
};
