import { css } from '@emotion/css/macro';

const crudButton = css`
  margin-right: 4px;
  cursor: pointer;
`;

const checkboxWrapper = css`
  text-align:center;
`;

export default { crudButton, checkboxWrapper };
